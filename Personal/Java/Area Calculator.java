public class Main {
	public static void main(String[] args) {
		double base = 7.6, height = 8.1;
		double area;
		area = base * height;
		System.out.println("The area of the rectangule with " + base + " as the base and " + height + " as the height is " + area);
	}
}
