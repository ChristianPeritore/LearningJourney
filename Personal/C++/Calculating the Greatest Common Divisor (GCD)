#include <iostream>
using namespace std;
int main () {
    int a, b, r;
    cout << "Enter 2 POSITIVE INTEGER numbers" << endl;
    cin >> a >> b;
    while (b != 0) {
        r = a % b;
        a = b;
        b = r;
    }
    cout << "The GCD is " << a << endl;
    return 0;
}
