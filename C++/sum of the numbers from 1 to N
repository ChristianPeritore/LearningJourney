#include <iostream>
using namespace std;
int main() {
    int N; 
    int S = 0;
    int I = 1;
    cout<<"Enter an integer"<<endl;
    cin>>N;
    while (I <= N) {
        S = S + I;
        I++;
    }
    cout<<"The sum of the numbers from 1 to "<<N<<" is "<<S<<endl;
    return 0;
}
